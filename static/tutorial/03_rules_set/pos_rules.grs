package POS {
  rule adj {
    pattern { X [upos=A] }
    commands { X.upos = ADJ }
  }

  rule noun {
    pattern { X [upos=N] }
    commands { X.upos = NOUN }
  }

  rule prep {
    pattern { X [upos=P] }
    commands { X.upos = ADP }
  }
}