module M {
  rule r {
    match { a[phon="trèfle"]; b[phon="coeur"]; c[phon="carreau"]; a -[A]-> b; }
    commands {
      shift_in a =[D]=> b;
      del_node a;
      shift_in b =[D]=> c;
    }
  }
}

sequences { main { M } }
