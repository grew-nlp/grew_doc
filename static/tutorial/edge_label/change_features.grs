rule change_features {
  pattern { e: GOV -[obj:lvc]-> DEP } 
  commands {
    e.1 = comp;    % change the edge feature '1' 
    e.2 = obj;     % change the edge feature '2'
    e.deep = lvc;  % change the edge feature 'deep'
  }
}
